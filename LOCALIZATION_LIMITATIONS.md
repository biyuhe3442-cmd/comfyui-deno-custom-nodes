# 简体中文本地化限制

本 Fork 采用显示层中文化：用户看见中文，程序仍接收原始内部值。这样既能完整显示中文，也不需要改动算法、接口或导出逻辑。

|位置|保留原因|风险|建议的后续方案|
|---|---|---|---|
|缩放方式、插值、比较模式等下拉选项|这些字符串是 Python 分支的实际 enum value|直接改值会改变节点执行分支|使用仅改选项文字、不改 option value 的显示层|
|模型名、文件名、URL、API/JSON key|它们是可复制的技术标识或协议字段|翻译会损害加载、请求和排错|保持原样；在相邻 tooltip 中用中文解释|
|traceback、异常原文、诊断报告字段|需要用于 GitHub/搜索/AI 排错|翻译会丢失可检索性|只翻译外围界面和错误说明，保留原始错误|
|GitHub Pages Web Tools 的完整页面正文|页面包含大量与文件处理紧密相邻的脚本字符串，不能进行全局替换|误替换可能改变导出、编码或格式判断|逐页建立 DOM `data-i18n` 键后再翻译，并对每种导出格式做手工回归|

这不是遗漏：任何可能改变节点算法、API 或执行逻辑的汉化都被明确拒绝；旧工作流兼容性不再作为项目目标。
